---
import { cn } from "@lib/utils";

/** Markdown/MDX typography wrapper. Tokens are wired in global.css `.prose`. */
type Props = { class?: string };
const { class: className } = Astro.props;
---

<div class={cn("prose", className)}>
  <slot />
</div>
